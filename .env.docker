# Frontend Environment Variables for Docker
# This file is used when running inside Docker containers
# The docker-compose.yml will override these with the proper container hostname
VITE_API_URL=http://backend:5001/api
